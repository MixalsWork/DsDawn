name: Auto Merge Live to Development

on:
  push:
    branches:
      - live

jobs:
  auto-merge:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Fetch all branches
        run: git fetch --all

      - name: Switch to development branch
        run: git checkout development

      - name: Merge live into development
        run: |
          git merge origin/live --no-ff --commit -m "Auto-merge live into development [skip ci]" || echo "No changes to merge"

      - name: Push changes to development branch
        run: git push origin development
